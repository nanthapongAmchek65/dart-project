void main() {
  // ลูปพิมพ์ตัวเลขจาก 1 ถึง 100
  for (int i = 1; i <= 100; i++) {
    // ตรวจสอบว่าเลขที่พิมพ์คือ 41 หรือไม่
    if (i == 41) {
      continue; // ถ้าเลขเป็น 41 ให้ข้ามไปที่รอบถัดไป
    }
    print(i); // พิมพ์ตัวเลขที่ไม่ใช่ 41
  }
}
